# 语音模块开发计划书

> **项目名称**：AI 模拟面试系统 — 语音模块
> **技术栈**：Vue 3 + Spring Boot 3 + Baidu Speech API
> **创建日期**：2026-06-07
> **版本**：v2.0（基于项目实际代码完善）
> **状态**：待评审 — API Key 待用户提供后填入

---

## 目录

1. [项目背景](#1-项目背景)
2. [需求分析](#2-需求分析)
3. [技术选型](#3-技术选型)
4. [系统架构](#4-系统架构)
5. [数据流设计](#5-数据流设计)
6. [详细实现计划](#6-详细实现计划)
7. [文件清单](#7-文件清单)
8. [工作量估算](#8-工作量估算)
9. [风险与应对](#9-风险与应对)
10. [验收标准](#10-验收标准)

---

## 1. 项目背景

AI 模拟面试系统目前支持**纯文本交互**：
- 用户文字输入回答
- AI（DeepSeek）返回文字回答（Markdown 格式渲染）
- 用户只能通过键盘输入

本项目旨在为面试系统增加**语音交互能力**，让用户可以通过语音回答问题，并听到 AI 的语音回复，模拟真实面试场景。

---

## 2. 需求分析

### 2.1 功能需求

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-01 | 语音输入（ASR） | 用户点击录音按钮，说话后自动转为文字，作为面试回答发送 | P0 |
| F-02 | 语音播放（TTS） | AI 回复后自动合成语音并播放，用户可听到面试官语音 | P0 |
| F-03 | 语音播放控制 | 每条 AI 消息附带播放/暂停/进度条控制 | P1 |
| F-04 | 文字/语音混合输入 | 用户可选择打字或语音，同一场面试自由切换 | P0 |
| F-05 | 录音可视化 | 录音时显示音量波形动画 | P2 |
| F-06 | 音频文件上传 | 支持上传已有录音文件进行识别 | P2 |
| F-07 | 自动播放开关 | 用户可控制是否自动播放 TTS 语音 | P1 |
| F-08 | 语速/音色设置 | 用户可调整 TTS 语速和发音人 | P2 |

### 2.2 非功能需求

| 编号 | 需求 | 指标 |
|------|------|------|
| N-01 | 录音延迟 | 点击录音按钮后 500ms 内开始录制 |
| N-02 | ASR 响应时间 | 录音结束后 2s 内返回识别结果 |
| N-03 | TTS 响应时间 | 文本生成后 1.5s 内返回音频 |
| N-04 | 音频质量 | 16kHz 采样率，单声道，16bit |
| N-05 | 兼容性 | 支持 Chrome/Edge/Firefox 最新版本 |
| N-06 | 安全性 | API Key 存储在服务端，不暴露给前端 |

---

## 3. 技术选型

### 3.1 语音服务：百度语音 API

| 服务 | 接口 | 说明 |
|------|------|------|
| 短语音识别 | `https://vop.baidu.com/server_api` | 支持 PCM/WAV/M4A，60s 内音频 |
| 在线语音合成 | `https://tsn.baidu.com/text2audio` | 支持 MP3/WAV 输出，1024 GBK 字节以内 |
| 鉴权 | `https://aip.baidubce.com/oauth/2.0/token` | Access Token 有效期 30 天 |

**百度 ASR 两种上传方式选择**：

| 方式 | 说明 | 选择 |
|------|------|------|
| JSON 格式上传 | 音频 Base64 编码，体积增大约 1/3 | 不选 |
| RAW 格式上传 | 音频原始二进制流放入 Body，参数通过 Header+URL 传递 | **选用** |

选择 RAW 方式原因：无 Base64 编码开销，减少传输数据量，后端直接将前端上传的音频字节转发给百度。

**百度 TTS 两种请求方式选择**：

| 方式 | 说明 | 选择 |
|------|------|------|
| GET | 参数拼 URL，浏览器可直接播放 | 不选 |
| POST | 表单格式请求体，无 URL 长度限制 | **选用** |

选择 POST 方式原因：`tex` 参数可能较长，POST 无 URL 长度限制，安全性更高。

### 3.2 前端录音：MediaRecorder API（Web API）

```
navigator.mediaDevices.getUserMedia({ audio: true })
  → MediaStream → MediaRecorder
  → 输出: audio/webm (Chrome) 或 audio/wav
```

- **优点**：浏览器原生 API，零依赖
- **支持度**：Chrome 49+ / Edge 79+ / Firefox 36+ / Safari 14.1+

### 3.3 音频播放：HTML5 `<audio>` 标签

- 百度 TTS 返回 MP3 格式
- `<audio>` 标签原生支持 MP3 解码播放
- 不需要 FFmpeg 或其他转码工具

### 3.4 选型结论

| 选项 | 选择 | 原因 |
|------|------|------|
| FFmpeg | **不需要** | 浏览器直接输出 PCM，百度直接接受 PCM，浏览器直接播放 MP3 |
| 百度 SDK | **不用 SDK** | 用 REST API 直调，避免 Maven 额外依赖，控制更灵活 |
| 后端 TTS | **后端统一处理** | API Key 不暴露到前端，统一缓存和错误处理 |
| 前端 ASR | **前端录音 → 后端识别** | 录音在前端完成，识别请求走后端 |

---

## 4. 系统架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                          前端（Vue 3）                              │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │              面试会话页面 (interview/session/index.vue)        │ │
│  │  ┌─────────┐  ┌──────────┐  ┌────────────┐  ┌────────────┐   │ │
│  │  │ 消息列表 │  │ 语音录制  │  │ TTS 播放器 │  │ 文本输入框  │   │ │
│  │  │ (显示)  │  │ (录制)   │  │ (播放)    │  │ (输入)     │   │ │
│  │  └────┬────┘  └────┬─────┘  └─────┬──────┘  └──────┬─────┘   │ │
│  │       │            │              │                │          │ │
│  │       ▼            ▼              ▼                ▼          │ │
│  │  ┌──────────────────────────────────────────────────────────┐  │ │
│  │  │                    API 调用层                             │  │ │
│  │  │  - sendMessageStream (SSE)  ← 流式 AI 回复               │  │ │
│  │  │  - uploadAudio (POST /multipart)  ← 录音上传             │  │ │
│  │  │  - fetchTts (POST)          ← 获取 TTS 音频              │  │ │
│  │  └──────────────────────┬───────────────────────────────────┘  │ │
│  └─────────────────────────┼───────────────────────────────────────┘ │
└────────────────────────────┼─────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       后端（Spring Boot 3.5）                       │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                     Controller 层                              │ │
│  │  ┌──────────────────┐  ┌────────────────────────────────────┐ │ │
│  │  │ InterviewController│  │  SpeechController (新增)          │ │ │
│  │  │  改造: SSE meta   │  │  POST /api/speech/asr             │ │ │
│  │  │  增加 TTS 音频URL │  │  POST /api/speech/tts             │ │ │
│  │  └─────────┬──────────┘  └─────────────┬────────────────────┘ │ │
│  └────────────┼────────────────────────────┼─────────────────────┘ │
│               │                            │                       │
│               ▼                            ▼                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │                     Service 层                                 │ │
│  │  ┌────────────────────┐  ┌────────────────┐  ┌──────────────┐ │ │
│  │  │ BaiduSpeechService │  │ AIService      │  │InterviewSrv  │ │ │
│  │  │  (新增)            │  │ (DeepSeek AI)  │  │ (已有)       │ │ │
│  │  │ ├─ getAccessToken  │  │ ├─ chat         │  │              │ │ │
│  │  │ ├─ speechToText    │  │ ├─ chatStream   │  │              │ │ │
│  │  │ ├─ textToSpeech    │  │ └─ ...          │  │              │ │ │
│  │  └─────────┬──────────┘  └────────────────┘  └──────────────┘ │ │
│  └────────────┼───────────────────────────────────────────────────┘ │
│               │                                                     │
│  ┌────────────┼──────────────────────────────────────────────────┐  │
│  │  Redis     │  baidu:speech:access_token (缓存 Token)          │  │
│  └────────────┴──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       百度语音 API                                   │
│                                                                     │
│  ┌────────────────────┐      ┌────────────────────┐                 │
│  │ 语音识别 (ASR)      │      │ 语音合成 (TTS)      │                 │
│  │ vop.baidu.com/     │      │ tsn.baidu.com/      │                 │
│  │ server_api         │      │ text2audio          │                 │
│  └────────────────────┘      └────────────────────┘                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 5. 数据流设计

### 5.1 完整交互流程（语音输入 + 语音输出）

```
用户说话
    │
    ▼
┌─────────────────────┐
│ 前端: MediaRecorder  │  录音期间显示音量波形
│ 录音开始             │
└─────────────────────┘
    │
    │ 用户停止说话（或点击停止）
    ▼
┌─────────────────────┐
│ 前端: AudioContext   │  解码 + 重采样为 16kHz PCM
│ convertToPcm()       │  输出 audio/pcm Blob
└─────────────────────┘
    │
    │ POST /api/speech/asr (multipart/form-data)
    ▼
┌─────────────────────┐
│ 后端: BaiduSpeech    │  调用百度 ASR REST API (RAW 方式)
│ .speechToText()      │  Content-Type: audio/pcm;rate=16000
└─────────────────────┘
    │
    │ 返回 { code: 200, data: { text: "用户说的内容" } }
    ▼
┌─────────────────────┐
│ 前端: 文字填入输入框  │  用户可编辑后发送，或直接自动发送
│ 或自动发送           │
└─────────────────────┘
    │
    │ POST /api/interview/{id}/stream (SSE)
    ▼
┌─────────────────────┐
│ 后端: InterviewSrv   │  DeepSeek AI 生成面试评价 + 下一题
│ .sendMessageStream() │  SSE chunk 逐字推送
└─────────────────────┘
    │
    │ SSE event: chunk (逐字), event: meta (完成)
    ▼
┌─────────────────────┐
│ 前端: 显示 AI 文本   │  实时渲染 Markdown
└─────────────────────┘
    │
    │ 收到 meta 后，前端异步请求 TTS
    ▼
┌─────────────────────┐
│ 前端: 调用 TTS       │  POST /api/speech/tts { text: "..." }
│ fetchTts()           │
└─────────────────────┘
    │
    │ 返回 MP3 音频二进制 (audio/mp3)
    ▼
┌─────────────────────┐
│ 前端: <audio> 播放   │  显示播放进度条
└─────────────────────┘
```

### 5.2 TTS 集成方案选择

**方案对比**：

| 方案 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| A: 后端 TTS 嵌入 SSE meta | meta 事件中附带 base64 音频 | 前端简单，一次请求 | SSE 响应变慢，base64 体积大 |
| B: 前端异步请求 TTS | meta 后前端单独 POST /tts | 不阻塞 SSE，按需加载 | 多一次请求 |

**最终选择：方案 B（前端异步请求 TTS）**

原因：
1. 不阻塞 SSE 流式输出，AI 文字可以先展示
2. 用户可能不需要每条都听语音，按需请求更节省
3. 避免 base64 编码导致 meta 事件体积膨胀
4. TTS 请求失败不影响主流程

### 5.3 SSE Meta 事件格式（不变）

当前 `StreamMetaVO` 格式保持不变，不增加 TTS 字段：

```json
{
  "type": "next_question",
  "nextQuestion": 2,
  "remainingQuestions": 3,
  "messageId": 123
}
```

前端在收到 meta 后，根据用户设置的"自动播放"选项，决定是否自动请求 TTS。

---

## 6. 详细实现计划

### Phase 1：后端 BaiduSpeechService

#### 1.1 创建 BaiduSpeechService.java

**文件路径**：`backend/src/main/java/org/backend/util/BaiduSpeechService.java`

**类结构**：

```java
@Service
public class BaiduSpeechService {

    private static final Logger logger = LoggerFactory.getLogger(BaiduSpeechService.class);

    @Value("${baidu.speech.api-key:}")
    private String apiKey;

    @Value("${baidu.speech.secret-key:}")
    private String secretKey;

    @Autowired
    private CacheService cacheService;  // 已有的 Redis 缓存服务

    private static final String TOKEN_CACHE_KEY = "baidu:speech:access_token";
    private static final String ASR_URL = "https://vop.baidu.com/server_api";
    private static final String TTS_URL = "https://tsn.baidu.com/text2audio";
    private static final String TOKEN_URL = "https://aip.baidubce.com/oauth/2.0/token";

    /** 单例 HttpClient，复用连接 */
    private final HttpClient httpClient = HttpClient.newBuilder()
            .connectTimeout(Duration.ofSeconds(10))
            .build();

    private final ObjectMapper objectMapper = new ObjectMapper();

    /**
     * 获取 Access Token（缓存到 Redis，有效期 30 天）
     * Redis Key: baidu:speech:access_token
     * 缓存策略：提前 1 天刷新，避免过期
     */
    public String getAccessToken() {
        // 1. 从 Redis 获取
        String cached = cacheService.get(TOKEN_CACHE_KEY, String.class);
        if (cached != null) return cached;

        // 2. 从百度获取
        String token = fetchAccessTokenFromBaidu();

        // 3. 缓存到 Redis（设置 29 天过期，token 有效期 30 天）
        cacheService.set(TOKEN_CACHE_KEY, token, 29 * 24 * 3600);

        return token;
    }

    /**
     * 语音识别：音频字节 → 文字
     * 使用百度 ASR RAW 方式（二进制流直接放入 Body）
     *
     * @param audioData  PCM/WAV 音频二进制数据
     * @param format     音频格式（pcm/wav）
     * @param rate       采样率（16000 推荐）
     * @param devPid     语言模型（1537=普通话, 1737=英语）
     * @return 识别结果文字，失败返回 null
     */
    public String speechToText(byte[] audioData, String format, int rate, int devPid) {
        try {
            String token = getAccessToken();
            String cuid = "ai-interview-system";

            String url = ASR_URL
                    + "?dev_pid=" + devPid
                    + "&token=" + token
                    + "&cuid=" + cuid;

            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create(url))
                    .header("Content-Type", "audio/" + format + ";rate=" + rate)
                    .POST(HttpRequest.BodyPublishers.ofByteArray(audioData))
                    .timeout(Duration.ofSeconds(10))
                    .build();

            HttpResponse<String> response = httpClient.send(
                    request, HttpResponse.BodyHandlers.ofString());

            JsonNode root = objectMapper.readTree(response.body());
            int errNo = root.path("err_no").asInt();

            if (errNo == 0) {
                JsonNode resultNode = root.path("result");
                if (resultNode.isArray() && resultNode.size() > 0) {
                    return resultNode.get(0).asText();
                }
            }

            logger.warn("ASR 识别失败: err_no={}, err_msg={}", errNo,
                    root.path("err_msg").asText());
            return null;
        } catch (Exception e) {
            logger.error("ASR 调用异常: {}", e.getMessage());
            return null;
        }
    }

    /**
     * 语音合成：文字 → MP3 音频字节
     * 使用百度 TTS POST 方式
     *
     * @param text  待合成文本（≤500 字符，超过截断）
     * @param per   发音人（0=度小美, 1=度小宇, 3=度逍遥, 4=度丫丫, 5003=度逍遥精品）
     * @param spd   语速（0-15, 5=正常）
     * @param pit   音调（0-15, 5=正常）
     * @param vol   音量（0-15, 5=正常）
     * @return MP3 音频二进制数据，失败返回 null
     */
    public byte[] textToSpeech(String text, int per, int spd, int pit, int vol) {
        try {
            String token = getAccessToken();
            if (text == null || text.trim().isEmpty()) return null;

            // 截断超长文本（百度 TTS 限制 1024 GBK 字节，建议 120 以内）
            if (text.length() > 500) {
                text = text.substring(0, 500);
            }

            // 两次 URL 编码（百度要求）
            String encodedText = URLEncoder.encode(
                    URLEncoder.encode(text, StandardCharsets.UTF_8), StandardCharsets.UTF_8);

            String cuid = "ai-interview-system";
            String formBody = "tok=" + token
                    + "&tex=" + encodedText
                    + "&cuid=" + cuid
                    + "&ctp=1&lan=zh"
                    + "&spd=" + spd
                    + "&pit=" + pit
                    + "&vol=" + vol
                    + "&per=" + per
                    + "&aue=3";  // 3=MP3

            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create(TTS_URL))
                    .header("Content-Type", "application/x-www-form-urlencoded")
                    .POST(HttpRequest.BodyPublishers.ofString(formBody))
                    .timeout(Duration.ofSeconds(10))
                    .build();

            HttpResponse<byte[]> response = httpClient.send(
                    request, HttpResponse.BodyHandlers.ofByteArray());

            // 成功: Content-Type 以 "audio/" 开头
            String contentType = response.headers()
                    .firstValue("Content-Type").orElse("");
            if (contentType.startsWith("audio/")) {
                return response.body();
            }

            // 失败: 返回 JSON 错误信息
            String errorJson = new String(response.body(), StandardCharsets.UTF_8);
            logger.warn("TTS 合成失败: {}", errorJson);
            return null;
        } catch (Exception e) {
            logger.error("TTS 调用异常: {}", e.getMessage());
            return null;
        }
    }

    /**
     * TTS 重载方法（面试场景默认参数）
     * 发音人: 4 (度丫丫), 语速: 5, 音调: 5, 音量: 5
     */
    public byte[] textToSpeech(String text) {
        return textToSpeech(text, 4, 5, 5, 5);
    }

    /**
     * 从百度 OAuth API 获取 Access Token
     */
    private String fetchAccessTokenFromBaidu() {
        try {
            String url = TOKEN_URL
                    + "?grant_type=client_credentials"
                    + "&client_id=" + apiKey
                    + "&client_secret=" + secretKey;

            HttpRequest request = HttpRequest.newBuilder()
                    .uri(URI.create(url))
                    .POST(HttpRequest.BodyPublishers.noBody())
                    .timeout(Duration.ofSeconds(10))
                    .build();

            HttpResponse<String> response = httpClient.send(
                    request, HttpResponse.BodyHandlers.ofString());

            JsonNode root = objectMapper.readTree(response.body());
            String token = root.path("access_token").asText();
            if (token == null || token.isEmpty()) {
                throw new BusinessException(500, "获取百度 Access Token 失败: "
                        + root.path("error_description").asText());
            }
            return token;
        } catch (BusinessException e) {
            throw e;
        } catch (Exception e) {
            throw new BusinessException(500, "百度 Token 请求异常: " + e.getMessage());
        }
    }
}
```

#### 1.2 application.yml 新增配置

在现有 `application.yml` 末尾追加：

```yaml
baidu:
  speech:
    api-key: ${BAIDU_SPEECH_API_KEY:}
    secret-key: ${BAIDU_SPEECH_SECRET_KEY:}
    # 默认 TTS 参数
    tts:
      per: 4       # 度丫丫（情感合成，适合面试场景）
      spd: 5
      pit: 5
      vol: 5
    # 默认 ASR 参数
    asr:
      dev-pid: 1537   # 普通话
      format: pcm
      rate: 16000
```

> **注意**：`api-key` 和 `secret-key` 当前为空，等待用户提供后填入。可通过环境变量 `BAIDU_SPEECH_API_KEY` 和 `BAIDU_SPEECH_SECRET_KEY` 注入。

#### 1.3 ASR 调用细节

**百度 ASR API（Raw Binary 方式）**：

```
POST https://vop.baidu.com/server_api?dev_pid=1537&token={access_token}&cuid=ai-interview-system
Content-Type: audio/pcm;rate=16000

{二进制 PCM 数据}
```

关键点：
- `len` 字段在 RAW 方式下不需要，服务端自动识别
- `cuid` 固定为 `ai-interview-system`，用于统计 UV
- `dev_pid=1537` 为普通话模型（默认值），支持自定义词库

#### 1.4 TTS 调用细节

**百度 TTS API（POST 方式）**：

```
POST https://tsn.baidu.com/text2audio
Content-Type: application/x-www-form-urlencoded

tok={access_token}&tex={二次URL编码文本}&cuid=ai-interview-system&ctp=1&lan=zh&spd=5&pit=5&vol=5&per=4&aue=3
```

关键点：
- `tex` 参数必须进行**两次 URLEncode**
- `ctp=1` 表示 Web 端客户端
- `lan=zh` 表示中英文混合
- `aue=3` 表示输出 MP3 格式
- 成功时 `Content-Type` 以 `audio/` 开头，响应体为音频二进制
- 失败时 `Content-Type` 为 `application/json`，响应体为错误信息

#### 1.5 Access Token 缓存策略

```java
public String getAccessToken() {
    // 1. 从 Redis 获取
    String cached = cacheService.get(TOKEN_CACHE_KEY, String.class);
    if (cached != null) return cached;

    // 2. 从百度获取
    String token = fetchAccessTokenFromBaidu();

    // 3. 缓存到 Redis（设置 29 天过期，token 有效期 30 天）
    cacheService.set(TOKEN_CACHE_KEY, token, 29 * 24 * 3600);

    return token;
}
```

利用项目已有的 `CacheService`（基于 `RedisTemplate`），无需新增依赖。

---

### Phase 2：后端 Controller 端点

#### 2.1 新增 SpeechController.java

**文件路径**：`backend/src/main/java/org/backend/controller/SpeechController.java`

```java
@RestController
@RequestMapping("/api/speech")
public class SpeechController extends BaseController {

    private final BaiduSpeechService baiduSpeechService;

    public SpeechController(BaiduSpeechService baiduSpeechService) {
        this.baiduSpeechService = baiduSpeechService;
    }

    /**
     * ASR：音频 → 文字
     * POST /api/speech/asr
     * Content-Type: multipart/form-data
     * 参数: audio (File), format (String, 可选, 默认 pcm)
     */
    @PostMapping("/asr")
    public Result<Map<String, Object>> asr(
            HttpServletRequest request,
            @RequestParam("audio") MultipartFile audioFile,
            @RequestParam(value = "format", defaultValue = "pcm") String format) {

        Long userId = getUserIdFromToken(request);

        if (audioFile.isEmpty()) {
            return Result.error(400, "音频文件为空");
        }
        // 检查文件大小（最大 2MB，60s PCM ≈ 960KB）
        if (audioFile.getSize() > 2 * 1024 * 1024) {
            return Result.error(400, "音频文件过大，请控制在 2MB 以内");
        }

        try {
            byte[] audioData = audioFile.getBytes();
            String text = baiduSpeechService.speechToText(audioData, format, 16000, 1537);

            Map<String, Object> data = new HashMap<>();
            data.put("text", text != null ? text : "");
            data.put("format", format);
            return Result.success(data);
        } catch (Exception e) {
            logger.error("语音识别失败", e);
            return Result.error(500, "语音识别失败: " + e.getMessage());
        }
    }

    /**
     * TTS：文字 → 音频
     * POST /api/speech/tts
     * Content-Type: application/json
     * 参数: { text: "...", per?: 4, spd?: 5, pit?: 5, vol?: 5 }
     * 返回: audio/mp3 二进制流
     */
    @PostMapping(value = "/tts", produces = "audio/mp3")
    public ResponseEntity<byte[]> tts(
            HttpServletRequest request,
            @RequestBody Map<String, Object> body) {

        getUserIdFromToken(request);  // 验证身份

        String text = (String) body.get("text");
        if (text == null || text.trim().isEmpty()) {
            return ResponseEntity.badRequest().build();
        }

        int per = body.containsKey("per") ? ((Number) body.get("per")).intValue() : 4;
        int spd = body.containsKey("spd") ? ((Number) body.get("spd")).intValue() : 5;
        int pit = body.containsKey("pit") ? ((Number) body.get("pit")).intValue() : 5;
        int vol = body.containsKey("vol") ? ((Number) body.get("vol")).intValue() : 5;

        byte[] audioData = baiduSpeechService.textToSpeech(text, per, spd, pit, vol);
        if (audioData == null) {
            return ResponseEntity.internalServerError().build();
        }

        return ResponseEntity.ok()
                .header("Content-Type", "audio/mp3")
                .header("Content-Length", String.valueOf(audioData.length))
                .body(audioData);
    }
}
```

**关键设计**：
- ASR 端点返回 `Result<Map>` 格式，与项目其他 API 保持一致
- TTS 端点返回 `ResponseEntity<byte[]>`，直接返回音频二进制流
- 两个端点都继承 `BaseController`，通过 `getUserIdFromToken` 验证身份
- TTS 参数支持可选自定义（per/spd/pit/vol），不传则使用默认值

#### 2.2 不修改 InterviewController

**重要决策**：不修改 `InterviewController` 和 `InterviewService.sendMessageStream()` 的 SSE 逻辑。

原因：
1. SSE 流式推送是核心链路，修改风险大
2. TTS 采用前端异步请求方案，不需要后端在 meta 中嵌入音频
3. 保持 `StreamMetaVO` 不变，前端收到 meta 后自行决定是否请求 TTS

---

### Phase 3：前端录音工具

#### 3.1 创建 audio.js 工具类

**文件路径**：`frontend/src/utils/audio.js`

```javascript
/**
 * 录音管理器
 *
 * 使用 MediaRecorder API 录制用户语音
 * 输出格式: audio/webm (Chrome) / audio/wav (可降级)
 *
 * 使用示例:
 *   const recorder = new AudioRecorder()
 *   await recorder.start()
 *   // ... 用户说话 ...
 *   const blob = await recorder.stop()
 *   const pcmBlob = await convertToPcm(blob)
 *   const text = await uploadAudio(pcmBlob)
 */
export class AudioRecorder {
  constructor(options = {}) {
    this.mediaRecorder = null
    this.audioChunks = []
    this.stream = null
    this.startTime = 0
    this.onVolumeChange = options.onVolumeChange || null  // 音量回调（可视化用）
    this.audioContext = null
    this.analyserNode = null
    this.mimeType = this._getSupportedMimeType()
  }

  /**
   * 获取浏览器支持的音频格式
   * 优先顺序: audio/webm;codecs=pcm > audio/webm > audio/wav
   */
  _getSupportedMimeType() {
    const types = [
      'audio/webm;codecs=pcm',
      'audio/webm',
      'audio/wav',
      'audio/ogg;codecs=opus'
    ]
    for (const type of types) {
      if (MediaRecorder.isTypeSupported(type)) return type
    }
    return ''
  }

  /**
   * 启动录音
   * 请求麦克风权限 → 创建 MediaRecorder → 开始录制
   */
  async start() {
    this.audioChunks = []
    this.stream = await navigator.mediaDevices.getUserMedia({
      audio: {
        sampleRate: 16000,   // 百度 ASR 推荐采样率
        channelCount: 1,     // 单声道
        echoCancellation: true,
        noiseSuppression: true
      }
    })

    // 创建 AnalyserNode 用于音量可视化
    if (this.onVolumeChange) {
      this.audioContext = new AudioContext()
      const source = this.audioContext.createMediaStreamSource(this.stream)
      this.analyserNode = this.audioContext.createAnalyser()
      this.analyserNode.fftSize = 256
      source.connect(this.analyserNode)
      this._startVolumeMeter()
    }

    return new Promise((resolve, reject) => {
      if (!this.mimeType) {
        reject(new Error('浏览器不支持音频录制'))
        return
      }

      this.mediaRecorder = new MediaRecorder(this.stream, {
        mimeType: this.mimeType
      })

      this.mediaRecorder.ondataavailable = (event) => {
        if (event.data.size > 0) {
          this.audioChunks.push(event.data)
        }
      }

      this.mediaRecorder.onerror = (event) => {
        reject(new Error('录音出错'))
      }

      this.mediaRecorder.start()
      this.startTime = Date.now()
      resolve()
    })
  }

  /**
   * 停止录音
   * @returns {Promise<Blob>} 音频 Blob
   */
  stop() {
    return new Promise((resolve) => {
      if (!this.mediaRecorder || this.mediaRecorder.state === 'inactive') {
        resolve(new Blob([], { type: this.mimeType }))
        return
      }

      this.mediaRecorder.onstop = () => {
        const blob = new Blob(this.audioChunks, { type: this.mimeType })
        this._cleanup()
        resolve(blob)
      }

      this.mediaRecorder.stop()
      // 停止所有音轨
      if (this.stream) {
        this.stream.getTracks().forEach(track => track.stop())
      }
    })
  }

  /**
   * 获取录音时长（秒）
   */
  getDuration() {
    if (!this.startTime) return 0
    return (Date.now() - this.startTime) / 1000
  }

  /**
   * 启动音量检测（用于可视化）
   */
  _startVolumeMeter() {
    const dataArray = new Uint8Array(this.analyserNode.frequencyBinCount)
    const tick = () => {
      if (!this.analyserNode) return
      this.analyserNode.getByteFrequencyData(dataArray)
      const average = dataArray.reduce((a, b) => a + b) / dataArray.length
      if (this.onVolumeChange) {
        this.onVolumeChange(average / 255)  // 归一化 0-1
      }
      requestAnimationFrame(tick)
    }
    tick()
  }

  /**
   * 清理资源
   */
  _cleanup() {
    if (this.audioContext) {
      this.audioContext.close()
      this.audioContext = null
    }
    this.analyserNode = null
    this.stream = null
    this.mediaRecorder = null
  }
}

/**
 * 将录音 Blob 转换为适合百度 ASR 的 PCM 格式
 * 百度 ASR 要求: 16kHz, 单声道, 16bit PCM
 *
 * @param {Blob} audioBlob  原始录音
 * @returns {Promise<Blob>} 转换后的 PCM Blob
 */
export async function convertToPcm(audioBlob) {
  // 如果已经是 PCM 格式，直接返回
  if (audioBlob.type.includes('pcm')) {
    return audioBlob
  }

  // 通过 AudioContext 解码并重采样为 16kHz PCM
  const arrayBuffer = await audioBlob.arrayBuffer()
  const audioContext = new AudioContext({ sampleRate: 16000 })
  const audioBuffer = await audioContext.decodeAudioData(arrayBuffer)

  // 提取 PCM 数据（16kHz, mono, 16bit）
  const channelData = audioBuffer.getChannelData(0)  // Float32Array
  const pcmData = new Int16Array(channelData.length)

  for (let i = 0; i < channelData.length; i++) {
    const s = Math.max(-1, Math.min(1, channelData[i]))
    pcmData[i] = s < 0 ? s * 0x8000 : s * 0x7FFF
  }

  audioContext.close()
  return new Blob([pcmData.buffer], { type: 'audio/pcm' })
}
```

#### 3.2 创建 speech.js API 文件

**文件路径**：`frontend/src/api/speech.js`

```javascript
import request from '@/utils/request'

/**
 * 上传音频进行语音识别（ASR）
 * @param {Blob} audioBlob  录音 Blob（PCM 格式）
 * @param {string} format   音频格式 (pcm/wav)
 * @returns {Promise<string>} 识别结果文字
 */
export function uploadAudio(audioBlob, format = 'pcm') {
  const formData = new FormData()
  formData.append('audio', audioBlob, `recording.${format}`)
  formData.append('format', format)

  return request({
    url: '/speech/asr',
    method: 'post',
    headers: { 'Content-Type': 'multipart/form-data' },
    data: formData
  }).then(res => {
    if (res.code === 200) return res.data.text
    throw new Error(res.message || '语音识别失败')
  })
}

/**
 * 获取 TTS 语音音频
 * @param {string} text  待合成文本
 * @param {Object} options  可选参数 { per, spd, pit, vol }
 * @returns {Promise<Blob>} MP3 音频 Blob
 */
export function fetchTts(text, options = {}) {
  return request({
    url: '/speech/tts',
    method: 'post',
    data: { text, ...options },
    responseType: 'blob'
  }).then(res => {
    // responseType: blob 时，axios 直接返回 Blob
    return res
  })
}
```

---

### Phase 4：面试界面集成

#### 4.1 录音按钮组件 VoiceRecorder.vue

**文件路径**：`frontend/src/views/interview/session/components/VoiceRecorder.vue`

```vue
<template>
  <div class="voice-recorder">
    <!-- 录音按钮 -->
    <button
      :class="['record-btn', { recording: isRecording, uploading: isUploading }]"
      @click="toggleRecord"
      :disabled="disabled || isUploading"
      :title="isRecording ? '点击停止录音' : '点击开始录音'"
    >
      <!-- 麦克风图标 -->
      <svg v-if="!isRecording && !isUploading" width="20" height="20" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z"/>
        <path d="M19 10v2a7 7 0 0 1-14 0v-2"/>
        <line x1="12" y1="19" x2="12" y2="23"/>
        <line x1="8" y1="23" x2="16" y2="23"/>
      </svg>
      <!-- 录音中动画 -->
      <div v-else-if="isRecording" class="recording-indicator">
        <span class="recording-dot"></span>
      </div>
      <!-- 上传中 -->
      <svg v-else width="20" height="20" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="2" class="spin">
        <path d="M21 12a9 9 0 1 1-6.219-8.56"/>
      </svg>
    </button>

    <!-- 录音状态提示 -->
    <div v-if="isRecording" class="record-status">
      <span class="record-dot"></span>
      <span class="record-time">{{ formatDuration(duration) }}</span>
      <!-- 音量可视化 -->
      <div class="volume-bar-container">
        <div class="volume-bar" v-for="i in 5" :key="i"
             :class="{ active: volume > i * 0.2 - 0.1 }"></div>
      </div>
    </div>

    <!-- 错误提示 -->
    <div v-if="error" class="record-error">{{ error }}</div>
  </div>
</template>

<script setup>
import { ref, onBeforeUnmount } from 'vue'
import { AudioRecorder, convertToPcm } from '@/utils/audio'
import { uploadAudio } from '@/api/speech'
import { ElMessage } from 'element-plus'

const props = defineProps({
  disabled: Boolean
})

const emit = defineEmits(['result', 'error'])

const isRecording = ref(false)
const isUploading = ref(false)
const duration = ref(0)
const volume = ref(0)
const error = ref('')

let recorder = null
let durationTimer = null

async function toggleRecord() {
  if (isRecording.value) {
    await stopRecord()
  } else {
    await startRecord()
  }
}

async function startRecord() {
  if (props.disabled) return
  error.value = ''

  try {
    recorder = new AudioRecorder({
      onVolumeChange: (v) => { volume.value = v }
    })
    await recorder.start()
    isRecording.value = true

    // 计时
    duration.value = 0
    durationTimer = setInterval(() => {
      duration.value = recorder.getDuration()
      // 超过 60 秒自动停止（百度 ASR 限制）
      if (duration.value >= 59) {
        stopRecord()
      }
    }, 200)
  } catch (e) {
    if (e.name === 'NotAllowedError' || e.name === 'PermissionDeniedError') {
      error.value = '麦克风权限被拒绝，请在浏览器设置中允许'
    } else if (e.name === 'NotFoundError') {
      error.value = '未检测到麦克风设备'
    } else {
      error.value = '启动录音失败：' + e.message
    }
    emit('error', error.value)
  }
}

async function stopRecord() {
  if (!recorder || !isRecording.value) return
  clearInterval(durationTimer)
  isRecording.value = false
  isUploading.value = true

  try {
    const audioBlob = await recorder.stop()

    if (audioBlob.size < 100) {
      error.value = '录音太短，请重新录制'
      isUploading.value = false
      return
    }

    // 转换为 PCM 格式
    const pcmBlob = await convertToPcm(audioBlob)

    // 上传识别
    emit('result', { status: 'uploading' })
    const text = await uploadAudio(pcmBlob, 'pcm')

    if (text && text.trim()) {
      emit('result', { status: 'success', text: text.trim() })
    } else {
      emit('result', { status: 'error', message: '未能识别到语音内容，请重新录制' })
      ElMessage.warning('未能识别到语音内容')
    }
  } catch (e) {
    emit('result', { status: 'error', message: e.message })
    ElMessage.error('语音识别失败：' + e.message)
  } finally {
    isUploading.value = false
  }
}

function formatDuration(seconds) {
  const m = Math.floor(seconds / 60)
  const s = Math.floor(seconds % 60)
  return `${m}:${s.toString().padStart(2, '0')}`
}

onBeforeUnmount(() => {
  if (recorder && isRecording.value) {
    recorder.stop()
  }
  clearInterval(durationTimer)
})
</script>

<style scoped>
.voice-recorder {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}

.record-btn {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  border: 2px solid #E4E4E7;
  background: #fff;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s;
  color: #71717A;
  flex-shrink: 0;
}

.record-btn:hover:not(:disabled) {
  border-color: #2563EB;
  color: #2563EB;
}

.record-btn.recording {
  background: #DC2626;
  border-color: #DC2626;
  color: #fff;
  animation: pulse 1.5s infinite;
}

.record-btn.uploading {
  border-color: #2563EB;
  color: #2563EB;
  cursor: wait;
}

.record-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

@keyframes pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(220, 38, 38, 0.4); }
  50% { box-shadow: 0 0 0 8px rgba(220, 38, 38, 0); }
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.spin {
  animation: spin 1s linear infinite;
}

.recording-indicator {
  display: flex;
  align-items: center;
  justify-content: center;
}

.recording-dot {
  width: 10px;
  height: 10px;
  background: #fff;
  border-radius: 50%;
}

.record-status {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: #DC2626;
}

.record-status .record-dot {
  width: 6px;
  height: 6px;
  background: #DC2626;
  border-radius: 50%;
  animation: blink 1s infinite;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}

.volume-bar-container {
  display: flex;
  gap: 2px;
  align-items: flex-end;
  height: 12px;
}

.volume-bar {
  width: 3px;
  height: 4px;
  background: #D4D4D8;
  border-radius: 1px;
  transition: all 0.1s;
}

.volume-bar.active {
  background: #DC2626;
  height: 12px;
}

.record-error {
  font-size: 11px;
  color: #DC2626;
  max-width: 120px;
  text-align: center;
  line-height: 1.3;
}
</style>
```

#### 4.2 TTS 播放控制组件 TtsPlayer.vue

**文件路径**：`frontend/src/views/interview/session/components/TtsPlayer.vue`

```vue
<template>
  <div class="tts-player" v-if="audioUrl">
    <audio
      ref="audioRef"
      :src="audioUrl"
      @ended="onEnded"
      @timeupdate="onTimeUpdate"
      @loadedmetadata="onLoaded"
      @error="onError"
      preload="metadata"
    ></audio>

    <button class="tts-btn" @click="togglePlay" :disabled="loading">
      <svg v-if="loading" width="14" height="14" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="2" class="spin">
        <path d="M21 12a9 9 0 1 1-6.219-8.56"/>
      </svg>
      <!-- 播放 -->
      <svg v-else-if="!playing" width="14" height="14" viewBox="0 0 24 24"
           fill="currentColor">
        <polygon points="5 3 19 12 5 21 5 3"/>
      </svg>
      <!-- 暂停 -->
      <svg v-else width="14" height="14" viewBox="0 0 24 24"
           fill="currentColor">
        <rect x="6" y="4" width="4" height="16"/>
        <rect x="14" y="4" width="4" height="16"/>
      </svg>
    </button>

    <div class="tts-progress" @click="seekAudio" ref="progressRef">
      <div class="tts-progress-fill" :style="{ width: progressPercent + '%' }"></div>
    </div>

    <span class="tts-time">{{ formatAudioTime(currentTime) }}/{{ formatAudioTime(duration) }}</span>
  </div>

  <!-- 无音频时显示加载按钮 -->
  <div class="tts-player tts-player-idle" v-else>
    <button class="tts-btn tts-load-btn" @click="loadAudio" :disabled="loading">
      <svg width="14" height="14" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="2">
        <polygon points="11 5 6 9 2 9 2 15 6 15 11 19 11 5"/>
        <path d="M15.54 8.46a5 5 0 0 1 0 7.07"/>
        <path d="M19.07 4.93a10 10 0 0 1 0 14.14"/>
      </svg>
    </button>
  </div>
</template>

<script setup>
import { ref, onBeforeUnmount } from 'vue'
import { fetchTts } from '@/api/speech'

const props = defineProps({
  text: { type: String, required: true },
  autoPlay: { type: Boolean, default: false }
})

const audioRef = ref(null)
const progressRef = ref(null)
const audioUrl = ref('')
const playing = ref(false)
const loading = ref(false)
const currentTime = ref(0)
const duration = ref(0)

async function loadAudio() {
  if (!props.text || loading.value) return
  loading.value = true

  try {
    const blob = await fetchTts(props.text)
    audioUrl.value = URL.createObjectURL(blob)

    if (props.autoPlay) {
      // 等待 audio 元素加载后自动播放
      setTimeout(() => {
        if (audioRef.value) {
          audioRef.value.play().catch(() => {})
          playing.value = true
        }
      }, 100)
    }
  } catch (e) {
    console.warn('TTS 加载失败', e)
  } finally {
    loading.value = false
  }
}

function togglePlay() {
  if (!audioRef.value) return

  if (playing.value) {
    audioRef.value.pause()
    playing.value = false
  } else {
    audioRef.value.play().catch(() => {})
    playing.value = true
  }
}

function onEnded() {
  playing.value = false
  currentTime.value = 0
}

function onTimeUpdate() {
  if (audioRef.value) {
    currentTime.value = audioRef.value.currentTime
  }
}

function onLoaded() {
  if (audioRef.value) {
    duration.value = audioRef.value.duration
  }
}

function onError() {
  loading.value = false
  playing.value = false
}

function seekAudio(event) {
  if (!audioRef.value || !progressRef.value) return
  const rect = progressRef.value.getBoundingClientRect()
  const x = event.clientX - rect.left
  const percent = x / rect.width
  audioRef.value.currentTime = percent * duration.value
}

const progressPercent = computed(() => {
  if (!duration.value) return 0
  return (currentTime.value / duration.value) * 100
})

function formatAudioTime(seconds) {
  if (!seconds || isNaN(seconds)) return '0:00'
  const m = Math.floor(seconds / 60)
  const s = Math.floor(seconds % 60)
  return `${m}:${s.toString().padStart(2, '0')}`
}

onBeforeUnmount(() => {
  if (audioUrl.value) {
    URL.revokeObjectURL(audioUrl.value)
  }
})
</script>

<style scoped>
.tts-player {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 8px;
  padding: 6px 10px;
  background: rgba(0, 0, 0, 0.03);
  border-radius: 8px;
}

.tts-player-idle {
  background: transparent;
  padding: 6px 4px;
}

.tts-btn {
  width: 28px;
  height: 28px;
  border-radius: 50%;
  border: none;
  background: #2563EB;
  color: #fff;
  cursor: pointer;
  font-size: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  transition: background 0.2s;
}

.tts-btn:hover:not(:disabled) {
  background: #1D4ED8;
}

.tts-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.tts-load-btn {
  background: transparent;
  color: #71717A;
  border: 1px solid #E4E4E7;
}

.tts-load-btn:hover:not(:disabled) {
  color: #2563EB;
  border-color: #2563EB;
  background: transparent;
}

.tts-progress {
  flex: 1;
  height: 4px;
  background: #D4D4D8;
  border-radius: 2px;
  cursor: pointer;
  overflow: hidden;
}

.tts-progress-fill {
  height: 100%;
  background: #2563EB;
  border-radius: 2px;
  transition: width 0.3s;
}

.tts-time {
  font-size: 11px;
  color: #71717A;
  white-space: nowrap;
  min-width: 60px;
  text-align: right;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

.spin {
  animation: spin 1s linear infinite;
}
</style>
```

#### 4.3 修改面试会话页面

**文件路径**：`frontend/src/views/interview/session/index.vue`

**改动点 1：Template — 输入区增加语音按钮**

在现有 `.input-area` 中，在 `textarea` 前面添加 VoiceRecorder 组件：

```html
<div class="input-area">
  <!-- 新增：语音录入按钮 -->
  <VoiceRecorder
    @result="handleVoiceResult"
    :disabled="sending"
    class="voice-btn-wrapper"
  />

  <textarea
    v-model="inputMessage"
    class="message-input"
    :placeholder="inputPlaceholder"
    @keydown.ctrl.enter="handleSend"
    :disabled="sending"
    rows="3"
  ></textarea>

  <button class="send-btn" @click="handleSend" :disabled="!inputMessage.trim() || sending">
    {{ sending ? '生成中' : '发送' }}
  </button>
</div>
```

**改动点 2：Template — AI 消息增加 TTS 播放器**

在 AI 消息气泡中，在 `.message-text` 后面添加 TtsPlayer 组件：

```html
<div class="message-bubble">
  <div v-if="msg.role === 'ai'" class="message-text markdown-body"
       v-html="renderMarkdown(msg.content)"></div>
  <div v-else class="message-text" v-html="formatMessage(msg.content)"></div>

  <!-- 新增：AI 消息的 TTS 播放控制 -->
  <TtsPlayer
    v-if="msg.role === 'ai' && msg.content"
    :text="msg.content"
    :autoPlay="autoPlayTts && msg.id === lastAiMessageId"
  />

  <div class="message-time" v-if="msg.createdAt">{{ formatTime(msg.createdAt) }}</div>
</div>
```

**改动点 3：Script — 新增 import 和逻辑**

```javascript
import VoiceRecorder from './components/VoiceRecorder.vue'
import TtsPlayer from './components/TtsPlayer.vue'

// 自动播放 TTS 开关
const autoPlayTts = ref(true)
const lastAiMessageId = ref(null)

// 语音识别结果回调
function handleVoiceResult(result) {
  if (result.status === 'success') {
    inputMessage.value = result.text
    // 方案：填入输入框让用户确认后发送（推荐）
    // 用户可以编辑识别结果后再发送
  } else if (result.status === 'error') {
    ElMessage.error('语音识别失败：' + result.message)
  }
}
```

**改动点 4：Script — SSE meta 处理中记录最后 AI 消息 ID**

在 `sendMessageStream` 的 meta 回调中：

```javascript
} else if (eventType === 'meta') {
  try {
    const meta = JSON.parse(data)
    // 原有逻辑...

    // 新增：记录最后一条 AI 消息 ID，用于自动播放 TTS
    if (meta.messageId) {
      lastAiMessageId.value = meta.messageId
    }

    resolve(meta)
  } catch (e) { ... }
}
```

**改动点 5：Style — 补充语音按钮样式**

```scss
.voice-btn-wrapper {
  flex-shrink: 0;
  align-self: flex-end;
  margin-bottom: 4px;
}
```

---

### Phase 5：配置与安全

#### 5.1 Spring Boot 文件上传限制

当前配置 `max-file-size: 2MB` 已够用（60s PCM ≈ 960KB），无需修改。

#### 5.2 环境变量管理

`application.yml` 中的百度密钥通过环境变量注入：

```yaml
baidu:
  speech:
    api-key: ${BAIDU_SPEECH_API_KEY:}
    secret-key: ${BAIDU_SPEECH_SECRET_KEY:}
```

在 `start.bat` 中配置（**等待用户提供 Key 后填入**）：

```batch
set BAIDU_SPEECH_API_KEY=【待填写】
set BAIDU_SPEECH_SECRET_KEY=【待填写】
```

#### 5.3 WebConfig 跨域配置

检查 `WebConfig.java` 是否需要为 `/api/speech/**` 路径添加跨域配置。由于项目已有全局跨域配置，通常不需要额外修改。

---

## 7. 文件清单

| # | 操作 | 文件路径 | 说明 |
|---|------|----------|------|
| 1 | **新增** | `backend/.../util/BaiduSpeechService.java` | ASR + TTS 核心服务 |
| 2 | **新增** | `backend/.../controller/SpeechController.java` | 语音端点 Controller |
| 3 | **修改** | `backend/src/main/resources/application.yml` | 追加百度语音配置 |
| 4 | **新增** | `frontend/src/utils/audio.js` | 录音工具类 + PCM 转换 |
| 5 | **新增** | `frontend/src/api/speech.js` | 语音 API 封装 |
| 6 | **新增** | `frontend/.../components/VoiceRecorder.vue` | 录音按钮组件 |
| 7 | **新增** | `frontend/.../components/TtsPlayer.vue` | TTS 播放控制组件 |
| 8 | **修改** | `frontend/.../session/index.vue` | 集成语音功能 |

**合计**：5 个新增文件 + 2 个修改文件 = 7 个文件

**不修改的文件**（相比 v1.0 方案的变更）：
- ~~InterviewController.java~~ — 不修改，TTS 由前端异步请求
- ~~InterviewService.java~~ — 不修改，SSE meta 格式不变
- ~~StreamMetaVO.java~~ — 不修改，不增加 TTS 字段

---

## 8. 工作量估算

| 阶段 | 内容 | 文件 | 预估工时 |
|------|------|:----:|:--------:|
| Phase 1 | BaiduSpeechService (ASR+TTS+Token) | 1 | 3h |
| Phase 2 | SpeechController | 1 | 1.5h |
| Phase 3 | 前端录音工具 + API | 2 | 2.5h |
| Phase 4 | VoiceRecorder + TtsPlayer + 页面集成 | 3 | 4h |
| Phase 5 | 配置 + 联调测试 | 1 | 2h |
| **合计** | | **8** | **13h** |

---

## 9. 风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|:----:|----------|
| 浏览器不兼容 MediaRecorder | 无法录音 | 低 | 降级为仅文本输入，提示用户更换浏览器 |
| 百度 ASR 返回空结果 | 语音无法转文字 | 中 | 前端提示用户重录或改用键盘输入 |
| 百度 TTS 合成失败 | 无法播放语音 | 低 | 静默降级，只展示文字，不中断面试流程 |
| Access Token 过期 | 全部语音功能失效 | 低 | Redis 缓存 + 提前刷新 + 异常时自动重试 |
| 录音文件太大超 60s 限制 | ASR 识别失败 | 中 | 前端控制录音时长 ≤ 59s，超时自动停止 |
| 麦克风权限被拒绝 | 无法录音 | 中 | 引导用户开启权限，同时保留文字输入 |
| AudioContext 重采样质量 | PCM 转换后识别率下降 | 低 | 使用 AudioContext 的 sampleRate 参数直接输出 16kHz |
| WebM 格式兼容性 | Chrome 录制 WebM 需转 PCM | 中 | convertToPcm() 统一转码，对用户透明 |

---

## 10. 验收标准

| 验收项 | 预期结果 |
|--------|----------|
| 点击录音按钮 | 开始录音，显示录音时长和音量动画 |
| 停止录音 | 自动上传并识别，5s 内返回文字 |
| 识别结果填入输入框 | 文字出现在输入框中，用户可编辑 |
| AI 回复后播放语音 | 点击播放按钮可听到 AI 语音 |
| 播放控制 | 可暂停/继续/拖动进度条 |
| 纯文字输入不受影响 | 不开启录音时原有输入方式正常工作 |
| 多轮面试交互 | 语音/TTS 在多轮对话中持续可用 |
| TTS 失败降级 | TTS 失败时只显示文字，不影响面试流程 |
| ASR 失败降级 | 识别失败时提示用户，可改用键盘输入 |

---

## 11. 待确认项

> 以下信息需要用户提供后才能完成开发：

| 项目 | 说明 | 状态 |
|------|------|------|
| BAIDU_SPEECH_API_KEY | 百度语音 API Key | **待提供** |
| BAIDU_SPEECH_SECRET_KEY | 百度语音 Secret Key | **待提供** |
| TTS 发音人选择 | 默认使用度丫丫(per=4)，是否需要更换 | 待确认 |
| 语音识别后自动发送 | 识别结果填入输入框后是否自动发送，还是等用户确认 | 待确认（默认需确认） |

---

> **文档版本**：v2.0
> **最后更新**：2026-06-08
> **状态**：待评审 — API Key 待用户提供后填入
